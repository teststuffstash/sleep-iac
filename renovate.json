{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":dependencyDashboard"],
  "labels": ["dependencies"],
  "rebaseWhen": "auto",
  "description": "Renovate here is for THIS repo's own dependencies only (GitHub Actions in .github/workflows, devbox tools). It does NOT bump the sleep-ingester chart/image pins — those are driven by the sleep-tracking `deploy` workflow's version-bump PR (homelab docs/sleep-iac.md §Deploy pipeline), keyed on git sha, which Renovate can't order."
}
