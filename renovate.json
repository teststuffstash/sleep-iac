{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":dependencyDashboard"],
  "labels": ["dependencies"],
  "rebaseWhen": "auto",
  "argocd": {
    "managerFilePatterns": ["/^apps/.+\\.ya?ml$/"]
  },
  "helm-values": {
    "managerFilePatterns": ["/^values/.+\\.ya?ml$/"]
  },
  "packageRules": [
    {
      "description": "sleep-ingester release: the OCI chart targetRevision (apps/, argocd manager) and the image.tag (values/, helm-values manager) move in lockstep, so group them into ONE version-bump PR. That PR *is* a deploy — manual review, gated by CI (chart==tag + render) + the reviewer/auto-merge path. FU-014.",
      "matchManagers": ["argocd", "helm-values"],
      "groupName": "sleep-ingester release",
      "automerge": false,
      "reviewers": ["sootrasmus"]
    }
  ]
}
